<MudTable Items="@Items" Hover="true" MultiSelection="true" SortLabel="Order" Elevation="0">
  <HeaderContent>
    <MudTh><MudTableSortLabel InitialDirection="SortDirection.Descending" SortBy="new Func<ActiveOrderRecord, object>(o => o.Time)">Time</MudTableSortLabel></MudTh>
    <MudTh><MudTableSortLabel SortBy="new Func<ActiveOrderRecord, object>(o => o.Name)">Instrument</MudTableSortLabel></MudTh>
    <MudTh><MudTableSortLabel SortBy="new Func<ActiveOrderRecord, object>(o => o.Size)">Size</MudTableSortLabel></MudTh>
    <MudTh><MudTableSortLabel SortBy="new Func<ActiveOrderRecord, object>(o => o.Side)">Side</MudTableSortLabel></MudTh>
    <MudTh><MudTableSortLabel SortBy="new Func<ActiveOrderRecord, object>(o => o.OpenPrice)">Open Price</MudTableSortLabel></MudTh>
    <MudTh><MudTableSortLabel SortBy="new Func<ActiveOrderRecord, object>(o => o.ClosePrice)">Close Price</MudTableSortLabel></MudTh>
    <MudTh><MudTableSortLabel SortBy="new Func<ActiveOrderRecord, object>(o => o.Gain)">Gain</MudTableSortLabel></MudTh>
  </HeaderContent>
  <RowTemplate>
    <MudTd DataLabel="Time">@($"{context.Time:yyyy-MM-dd HH:mm:ss}")</MudTd>
    <MudTd DataLabel="Instrument">@context.Name</MudTd>
    <MudTd DataLabel="Size">@context.Size</MudTd>
    <MudTd DataLabel="Side">@context.Side</MudTd>
    <MudTd DataLabel="Open Price">@($"{context.OpenPrice:0.00}")</MudTd>
    <MudTd DataLabel="Close Price">@($"{context.ClosePrice:0.00}")</MudTd>
    <MudTd DataLabel="Gain">@($"{context.Gain:0.00}")</MudTd>
  </RowTemplate>
  <PagerContent>
    <MudTablePager PageSizeOptions="new int[]{50, 100}" />
  </PagerContent>
</MudTable>
